name: CI/CD Pipeline

on:
  push:
    branches:
      - master
      - main  # Adjust if your main branch is named differently
  pull_request:
    branches:
      - master
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9  # Specify the Python version you need

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        pytest tests/  # Assuming you have tests written using pytest

  deploy:
    runs-on: ubuntu-latest
    needs: test  # Deploy only if tests pass

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Deploy to production
      run: |
        # Example deployment command, replace with your actual deployment command
        echo "Deploying to production..."
        # You can add your deployment scripts here, for example:
        # ./deploy.sh
